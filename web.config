<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <!-- Simplified config to avoid 500.19 error if URL Rewrite is not installed -->
  </system.webServer>
</configuration>
